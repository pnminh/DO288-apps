apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "review-todo.fullname" . }}-test-connection"
  labels:
    {{- include "review-todo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "review-todo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
